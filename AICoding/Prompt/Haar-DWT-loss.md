注意看diffusion-4K库（路径：diffusion-4k） 中采用的loss函数有些特别，其中我记得好像涉及到了小波变换，LL，LR，RL，RR之类的，分别对应高频和低频（我记得）。
文章内容节选：{Wavelet-based Latent Enhancement. Wavelet transform has achieved significant success in image processing and is widely used to decompose low-frequency approximations and high-frequency details from images or features [14, 33]. In this section, we propose wavelet-based latent enhance ment, which emphasizes high-frequency components while preserving low-frequency information, thereby significantly enhancing fine details and rich textures in 4K image gener ation. Consider the diffusion process, i.e. zt = αt ·x0 +σt ·ϵ, (1) where x0 and ϵ represent data distribution and standard normal distribution, respectively, and αt and σt are hyper parameters in the diffusion formulation. The original train ing objective in latent diffusion models is noise predic tion [18, 41], defined as: ϵΘ(zt,t) = ϵt, (2) where Θ denotes the neural network, e.g., convolutional UNet or DiT. Recent approaches, such as SD3 [12] and Flux [5], adopt rectified flows to predict velocity v parame terized by Θ, with the objective as follows: vΘ(zt,t) = ϵ −x0. (3) Our method decomposes the low-frequency approximation and high-frequency details of latent features using wavelet transform, defining the training objective as follows: LWLF =Ewt∥f(vΘ(zt,t))−f(ϵ−x0)∥2 , (4) where wt is the loss weight, and f(·) denotes discrete wavelet transform (DWT). Notably, we use the Haar wavelet, widely adopted in real-world applications due to its efficiency. Specifically, L = 1 √ √ 1 2 2 [1,1] and H = [−1,1] denote the low-pass and high-pass filters, used to construct four kernels in DWT with stride 2, namely LLT,LHT,HLT,HHT. The DWT kernels are then used to decompose the input features into four sub-bands, the low-frequency approximation xll and high-frequency com ponents xlh,xhl,xhh. As a result, both low-frequency in formation and high-frequency details are incorporated in Eq. (4), contributing to a comprehensive optimization in 4K image synthesis. In addition, our method supports various diffusion models by simply substituting the reconstruction objective, enabling seamless integration with conventional noise prediction approaches.}

这其中的Haar DWT给我了启发，我现在的图像细节部分缺少很多，而Haar DWT可以提取高频细节，我能否将这个技术应用于per-scale的监督？

同时，既然Haar DWT分低频和高频，能不能在per-scale中的低尺度多用低频loss，高尺度多用高频loss（使用权重）

注意更新skill，更新args，更新buaa_train_cmds（直接在文件末尾新增）。

记得生成commit信息