好的，注意看@backup1_stdout.txt (32520-32561) 这个训练过程，我将这个训练过程记为x，我认为stage1的任务应该已经差不多结束了。

接下来将进行下一个任务：stage2的训练。

进行stage2训练之前，首先需要你注意以下几点（以下也就是你这次的任务）：

1.刚刚训练出来的ckpt路径为：local_output/ckpt-3.pth 我希望你能够先将它找个地方放好并重新命名，stage2的训练需要用到它，以免未来找不到。
2.注意到路径/home/featurize/data/brats_256_t2_2021_pair_png_with_ref/test/LR，我不知道刚刚的训练过程x中有没有测试过测试数据，如果有，请指出代码，以及测试的结果，如果没有，则说明没有。
3.无论2的结果如何，我希望你能够帮我写个脚本，脚本功能为：测试某个stage1的ckpt（使用刚刚的test文件夹），
要求：a.随机测试test文件夹中100张图片.2.输出各自PSNR，SSIM到某个日志中（或许你可以新建一个日志）。3.输出100张图片的重建图，每张图4行两列，就和reconstruction_dir_name下的图片一样，左边一列为gt，右边一列为pred。这些图片放在哪里？希望你能够给我搞个文件夹放一放，（这个路径应该作为脚本参数）日志也应该放在这个文件夹下。
4.所有代码以及脚本务必写的好维护，不要太乱，可扩展性也应当不差。
5.如有必要，更新skill。